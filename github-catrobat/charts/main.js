"use strict";

[
    './handled-after',
    './merged-after-probability',
    './participants-per-changes',
    './pull-request-changes',
    './time-until-merge',
    './top-commenters',
].forEach(require);
